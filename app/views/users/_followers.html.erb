<ul><%= "Trusted by list" %>
<% @user.followers.each do |follower| %>
<%= render "users/user_profile_summary", :id => follower.id, :fb_pic => follower.fb_pic, :fb_id => follower.fb_id, :name => follower.name %>
<% end %>
</ul>

