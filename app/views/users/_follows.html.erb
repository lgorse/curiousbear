<ul><%= "trusts list" %>
<% @user.following.each do |followed| %>
<%= render "users/user_profile_summary", :id => followed.id, :fb_pic => followed.fb_pic, :fb_id => followed.fb_id, :name => followed.name %>
<% end %>
</ul>

