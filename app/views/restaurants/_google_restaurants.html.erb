<ul>
	<% @google_results.sort_by{|result| result["rating"].to_i}.reverse.each do |result| %>
	<%= render 'restaurants/restaurant_summary', :result => result, :venue => Restaurant.new(set_attr_from_google(result).merge(:id => nil)) %>
	
	<% end %>
</ul>