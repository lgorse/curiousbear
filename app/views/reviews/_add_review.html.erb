<% if Restaurant.find_by_google_id(@google_id) %>
<%= button_to "Update review", {:controller => 'reviews', :action => 'edit', :id => Review.find_by_restaurant_id_and_user_id(@restaurant.id, @current_user.id)}, :method => :get %>
	<% else %>
<%= form_tag reviews_new_path, :method => 'post', :id => "new_review" do  %>
<%= hidden_field_tag :search_field, @encoded_search %>
<%= hidden_field_tag :venue_field, @encoded_venue %>
<%= submit_tag "Add review" %>
	<% end %>
<% end %>