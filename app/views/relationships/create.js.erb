$(function(){
	$("#button_<%= @followed_id %>").html('<%= escape_javascript(render "relationships/buttons/unfollow_button", :followed_id => @followed_id) %>');	
	$("#follows_list_button").html('<%= "Trusts (#{@user.following.count})" %>');
	
});