<%= "https://graph.facebook.com/"+@me["id"]+"/permissions"+"?access_token="+session["fb_cookie"]["access_token"] %>
<%= form_for(@user) do |f| %>
<%= render 'user_edit_fields', :f => f %>
<%= f.submit "update" %>
<% end %>
<%= link_to 'unsubscribe', @user, :method => :delete, :confirm => "Are you sure?"  %>